<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/menuPrimary</item>
        <item name="colorPrimaryDark">@android:color/darker_gray</item>
        <item name="android:actionMenuTextColor">@color/menuText</item>
        <item name="actionOverflowButtonStyle">@style/BotaoMenuAzul</item>
    </style>

    <style name="BotaoMenuAzul" parent="@style/Widget.AppCompat.ActionButton.Overflow" >
        <item name="android:tint">@color/menuText</item>
    </style>


</resources>
