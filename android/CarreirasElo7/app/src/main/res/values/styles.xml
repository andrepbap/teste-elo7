<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/menuPrimary</item>
        <item name="colorPrimaryDark">@android:color/darker_gray</item>
        <item name="android:actionMenuTextColor">@color/menuText</item>
        <item name="actionOverflowButtonStyle">@style/AppTheme.BotaoMenu</item>
        <item name="actionBarStyle">@style/AppTheme.ActionBar</item>
    </style>

    <style name="AppTheme.BotaoMenu">
        <item name="android:tint">@color/menuText</item>
    </style>

    <style name="AppTheme.ActionBar">
        <item name="logo">@drawable/logo</item>
        <item name="displayOptions">useLogo|showHome</item>
    </style>


</resources>
